duedinoi_web_homepage:
    pattern: /{_locale}
    defaults: { _controller: DuedinoiWebBundle:Default:homepage, _locale: 'it' } 
duedinoi_edit_profile:
    pattern: /{_locale}/edit-profile
    defaults: { _controller: DuedinoiWebBundle:Default:editProfile, _locale: 'it' }
duedinoi_settings:
    pattern: /{_locale}/settings
    defaults: { _controller: DuedinoiWebBundle:Default:settings, _locale: 'it' }
duedinoi_dashboard:
    pattern: /{_locale}/dashboard
    defaults: { _controller: DuedinoiWebBundle:Default:dashboard, _locale: 'it' }
duedinoi_my_profile:
    pattern: /{_locale}/my-profile
    defaults: { _controller: DuedinoiWebBundle:Default:myProfile, _locale: 'it' }
duedinoi_contact:
    pattern: /{_locale}/contact-us
    defaults: { _controller: DuedinoiWebBundle:Default:contact, _locale: 'it' }
duedinoi_photos:
    pattern: /{_locale}/photos
    defaults: { _controller: DuedinoiWebBundle:Default:photos, _locale: 'it' }
duedinoi_profile_picture:
    pattern: /{_locale}/profile-picture/{id}
    defaults: { _controller: DuedinoiWebBundle:Default:profilePicture, _locale: 'it' }
duedinoi_messages:
    pattern: /{_locale}/messages
    defaults: { _controller: DuedinoiWebBundle:Default:messages, _locale: it }
duedinoi_thread:
    pattern: /{_locale}/messages/{userslug}
    defaults: { _controller: DuedinoiWebBundle:Default:thread, _locale: it }
duedinoi_user_profile:
    pattern: /{_locale}/{userslug}/profile
    defaults: { _controller: DuedinoiWebBundle:Default:userProfile, _locale: 'it' }
duedinoi_load_messages:
    pattern: /{_locale}/load-messages/{userslug}
    defaults: { _controller: DuedinoiWebBundle:Default:loadMessages, _locale: 'it' }
duedinoi_notifications:
    pattern: /{_locale}/notifications
    defaults: { _controller: DuedinoiWebBundle:Default:notifications, _locale: it }
duedinoi_about:
    pattern: /{_locale}/about-us
    defaults: { _controller: DuedinoiWebBundle:Default:about, _locale: it }
duedinoi_user_conversation:
    pattern: /{_locale}/{userslug}/conversation
    defaults: { _controller: DuedinoiWebBundle:Default:userConversation, _locale: 'it' }
duedinoi_chat_server:
    pattern: /{_locale}/{userslug}/chat-server
    defaults: { _controller: DuedinoiWebBundle:Default:chatServer, _locale: 'it' }
