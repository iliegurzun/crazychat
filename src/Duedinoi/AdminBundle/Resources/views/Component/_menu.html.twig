<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">  
    <ul class="nav navbar-nav">  
        <li class="{% if app.request.get('_route') == 'duedinoi_admin_homepage' %} active {% endif %}" ><a href="{{ path('duedinoi_admin_homepage') }}">Home</a></li>
        {% for key, item in get_admin_menu() %}
            <li class="
                {% if app.request.get('_route') in [item['route'], item['route']~'_show', item['route']~'_create', item['route']~'_new'] %}active {% endif %}">

                <a href="{{ path( item['route']) }}">{{ key }}</a>
            </li>  
        {% endfor %}
        <li class="dropdown">  
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"> Logged in as {{ app.user.username }}<b class="caret"></b></a>  
            <ul class="dropdown-menu">  
                <li class="socials">
                    <a href="{{ path('fos_user_security_logout') }}">
                        Logout
                    </a>
                </li>  
            </ul>  
        </li> 
    </ul>  
</nav>